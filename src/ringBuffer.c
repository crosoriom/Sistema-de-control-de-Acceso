#include "ringBuffer.h"

ring_buffer_t ring_buffer_init(int size)
{
    ring_buffer_t rb;

    rb.buffer = (char *)malloc(size * sizeof(char));
}

void ring_buffer_write()
{
}

void ring_buffer_read()
{
}
